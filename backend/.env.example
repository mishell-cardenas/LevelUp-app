# MongoDB connection string
# Replace <username>, <password>, and <cluster> with your own Atlas credentials
MONGO_URI="mongodb+srv://<username>:<password>@<cluster>.mongodb.net/gaminghub?retryWrites=true&w=majority"

# Server port
PORT=3000